import React from 'react';
import Layout from '../Layout/Layout';

function Home(props) {
    return (
        <Layout>
            Home - No login required
        </Layout>
    );
}

export default Home;
